<h1>Listing Voicemails</h1>

<table>
  <tr>
    <th>Voicemail</th>
  </tr>

  <% @voicemails.each do |voicemail| %>
      <tr>
        <td><%= voicemail.attachment.url %></td>
        <td><%= link_to 'Destroy', voicemail, :confirm => 'Are you sure?', :method => :delete %></td>
      </tr>
  <% end %>
</table>

<br />

<%= link_to 'New Voicemail', new_voicemail_path %>